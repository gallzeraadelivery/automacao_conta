FROM node:22-slim

RUN corepack enable

WORKDIR /app

COPY pnpm-workspace.yaml package.json pnpm-lock.yaml tsconfig.json ./
COPY apps/mock-server ./apps/mock-server

RUN pnpm install --frozen-lockfile

WORKDIR /app
EXPOSE 3001

CMD ["pnpm", "--filter", "@uber-automation/mock-server", "dev"]
